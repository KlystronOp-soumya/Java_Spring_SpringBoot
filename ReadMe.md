# Objective
Simple Java application using Azure Service Bus SDK to interact with the Service Bus
> Primary app to feed data to the Function app in local

#### How to

- run containers: docker compose up -d
- stop containers: docker compose down