CREATE TABLE IF NOT EXISTS  AGENT_TEST
(
    COLL_OFF char(3) NOT NULL DEFAULT '',
	AGENT_ID VARCHAR(10) NOT NULL DEFAULT '',
	AGENT_NO VARCHAR(9) NOT NULL DEFAULT '' ,
	POL_NO VARCHAR(6) NOT NULL DEFAULT '',
	LOB VARCHAR(2) NOT NULL DEFAULT '',
	COMMISSION NUMBER(11,2) NOT NULL
);

